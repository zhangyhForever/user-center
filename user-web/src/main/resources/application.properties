spring.sql.init.schema-locations=classpath*:db/schema.sql
spring.sql.init.data-locations=classpath*:db/data.sql

spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/user_center}
spring.datasource.username=${MYSQL_USER:usercenter}
spring.datasource.password=${MYSQL_PASS:usercenter}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.sql.init.mode=always

mybatis.mapper-locations=mapper/**.xml

spring.mvc.pathmatch.matching-strategy=ant_path_matcher

server.port=9090

############    mail config start #################
mail.send.config.enable=false
mail.send.config.from=
mail.send.config.title=
mail.send.config.content=
# ???????
spring.mail.host=smtp.qq.com
# ?????????
spring.mail.username=
# ???????????
spring.mail.password=
############    mail config end #################
